<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatis.mapper.RoleMenuMapper">

    <resultMap id="MenuResultMap" type="com.example.mybatis.entity.Menu">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="path" property="path"/>
        <result column="redirect" property="redirect"/>
        <result column="always_show" property="alwaysShow"/>
        <result column="hidden" property="hidden"/>
        <result column="title" property="title"/>
        <result column="icon" property="icon"/>
        <result column="no_cache" property="noCache"/>
        <result column="title_key" property="titleKey"/>
        <result column="link" property="link"/>
        <result column="component" property="component"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="parent_id" property="parentId"/>
        <result column="text_color" property="textColor"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
        <result column="username" property="username"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO role_menu (role_id, menu_id)
        VALUES (#{roleId}, #{menuId})
        ON CONFLICT (role_id, menu_id) DO NOTHING
    </insert>

    <delete id="deleteByRoleId">
        DELETE FROM role_menu
        WHERE role_id = #{roleId}
    </delete>

    <select id="selectMenusByRoleId" resultMap="MenuResultMap">
        SELECT m.id, m.name, m.path, m.redirect, m.always_show, m.hidden, m.title, m.icon,
               m.no_cache, m.title_key, m.link, m.component, m.sort_order, m.parent_id, m.text_color,
               m.created_at, m.updated_at, m.deleted_at, m.username
        FROM menu m
        INNER JOIN role_menu rm ON rm.menu_id = m.id
        WHERE rm.role_id = #{roleId}
        AND m.deleted_at IS NULL
        ORDER BY m.sort_order ASC NULLS LAST, m.id
    </select>
</mapper>
